# Cascade Indicators

Masked & subset MSD data on the clinical cascade for Saturn Operating
Unit (Total Numerators only). Data sourced from ICPI/TrainingDataset.

## Usage

``` r
cascade
```

## Format

A data frame with six variables: `operatingunit`, `prime_partner_name`,
`indicator`, `period`, `period_type`, and `value`.
