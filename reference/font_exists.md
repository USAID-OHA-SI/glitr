# Check if font exists

Check if font exists

## Usage

``` r
font_exists(font = c("Source Sans 3", "Source Sans Pro"))
```

## Arguments

- font:

  typeface to check, defaults to Source Sans 3 and Pro
